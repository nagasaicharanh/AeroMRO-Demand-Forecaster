# README Pending Items

The following README polish items are still pending due to missing assets in the repository:

1. Replace the ASCII architecture block with a proper architecture diagram image (PNG) and embed it in README.
   - *Status: Pending (Architecture PNG not found in repository)*
2. Add dashboard demo GIF and embed it:
   - `screenshots/demo.gif`
   - *Status: Pending (demo.gif not found in repository)*
3. Add SHAP screenshot:
   - `screenshots/xgboost_shap_summary.png`
   - *Status: Pending (SHAP screenshot not found in repository)*

Completed:
- [x] Add a **Model Performance** table with actual measured metrics.
- [x] Replace section image references with finalized screenshot assets and stable filenames.
- [x] Add missing top badges (Python 3.11, Docker Compose, MLflow, License, CI).
- [x] Mention SHAP explicitly in **Key Features**.
